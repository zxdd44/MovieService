# Movie Service (Spring Boot REST API)

Данный сервис был создан и предназначен для управления информацией о фильмах.
Ссылка на SonarCLoud: https://sonarcloud.io/summary/overall?id=zxdd44_MovieService&branch=main

## Составляющие программы

1. **Spring Boot приложение**: Проект инициализирован как Spring Boot приложение с использованием Spring Web.
2. **Предметная область (Domain)**: Основная сущность — `Movie`, включающая статус просмотра через Enum `MovieStatus`.
3. **Реализация REST API**:
    * **GET с @PathVariable**: Эндпоинт `/api/movies/{id}` для получения фильма по его идентификатору.
    * **GET с @RequestParam**: Эндпоинт `/api/movies/filter?director=...` для поиска фильмов по конкретному режиссеру.
4. **Слои приложения**:
    * **Controller**: `MovieController` — обрабатывает внешние HTTP-запросы.
    * **Service**: `MovieService` — содержит бизнес-логику поиска и фильтрации.
    * **Repository**: `MovieRepository` — обеспечивает доступ к данным (In-memory хранилище).
5. **DTO и Mapping**:
    * Объект `MovieDto` используется для возврата данных пользователю, скрывая детали реализации (например, мапит статус в числовой код).
    * `MovieMapper` инкапсулирует логику преобразования сущности `Movie` в `MovieDto`.
6. **Checkstyle**: Код приведен к единому стилю (Google Java Style). Добавлены Javadoc-комментарии для всех публичных методов и классов.

---

## 🛠 Техническое описание эндпоинтов

### 1. Получение по ID
* **URL**: `/api/movies/{id}`
* **Метод**: `GET`
* **Параметр пути**: `id` (Long)
* **Пример запроса**: `GET http://localhost:8080/api/movies/1`

### 2. Фильтрация по режиссеру
* **URL**: `/api/movies/filter`
* **Метод**: `GET`
* **Параметр запроса**: `director` (String)
* **Пример запроса**: `GET http://localhost:8080/api/movies/filter?director=Christopher%20Nolan`

---

## 📦 Структура данных (Status Codes)
В API статусы фильмов передаются в виде целых чисел:
* `0` — NOT_WATCHED (Не просмотрено)
* `1` — WATCHED (Просмотрено)
* `2` — DEFERRED (Отложено)
* `3` — ABANDONED (Брошено)
* 
### Клонирование репозитория

```bash
git clone https://github.com/zxdd44/MovieService.git
cd MovieService


